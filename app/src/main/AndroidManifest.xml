<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gtappdevelopers.videoplayerapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/multimedia"
        android:label="@string/app_name"
        android:roundIcon="@drawable/multimedia"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoPlayerApp">
        <activity android:name=".VideoPlayerActivity" android:theme="@style/VideoPlayerTheme"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>